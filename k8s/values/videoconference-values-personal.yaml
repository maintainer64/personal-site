apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: videoconference-personal-redirect-ingress
  annotations:
    nginx.ingress.kubernetes.io/permanent-redirect: "https://tochka.ktalk.ru/ateibq1yybd5"
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
    - hosts:
        - talk.gubanov.site
      secretName: talk-gubanov-site-tls
  rules:
    - host: talk.gubanov.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dummy-service
                port:
                  number: 80